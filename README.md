# 🗳️ نظام التصويت الإلكتروني - بني عبيد

نظام تصويت إلكتروني آمن وموثوق لمرشحي مجلس النواب في مركز بني عبيد، مكتوب بـ PHP و MySQL.

## ✨ الميزات الرئيسية

- 🎨 **تصميم احترافي** بألوان علم مصر
- 🔒 **نظام حماية قوي** لمنع التصويت المتكرر
- 📊 **عرض فوري للنتائج** مع رسوم بيانية
- 💾 **قاعدة بيانات آمنة** لتخزين الأصوات
- 📱 **واجهة استجابة** تعمل على جميع الأجهزة
- ⚡ **ملفات خفيفة جداً** - أقل من 5 MB

## 🚀 البدء السريع

### المتطلبات
- PHP 7.4 أو أحدث
- MySQL 5.7 أو أحدث
- استضافة تدعم PHP و MySQL (مثل Hostinger)

### خطوات التثبيت

#### 1. رفع الملفات
- حمّل جميع الملفات إلى استضافتك
- تأكد من أن جميع الملفات موجودة في المجلد الجذر

#### 2. إعداد قاعدة البيانات
- افتح `setup.php` من متصفحك
- أدخل بيانات قاعدة البيانات:
  - **عنوان قاعدة البيانات:** localhost (أو البيانات التي توفرها استضافتك)
  - **اسم المستخدم:** (من بيانات استضافتك)
  - **كلمة المرور:** (من بيانات استضافتك)
  - **اسم قاعدة البيانات:** voting_system (أو أي اسم تختاره)
- اضغط "إعداد النظام"

#### 3. حذف ملف الإعداد
- احذف ملف `setup.php` بعد الإعداد لأسباب أمنية

#### 4. الدخول للموقع
- افتح `index.php` من متصفحك
- يجب أن ترى الموقع يعمل بشكل كامل

## 📁 هيكل الملفات

```
beni-obeid-voting-php/
├── index.php              # الصفحة الرئيسية
├── setup.php              # صفحة الإعداد
├── api/
│   ├── get_candidates.php # API للحصول على المرشحين
│   ├── check_vote_status.php # API للتحقق من التصويت
│   └── vote.php           # API للتصويت
├── includes/
│   ├── config.php         # الإعدادات والثوابت
│   ├── db.php             # الاتصال بقاعدة البيانات
│   └── helpers.php        # دوال المساعدة
├── css/
│   └── style.css          # أنماط CSS
├── js/
│   └── app.js             # كود JavaScript
└── README.md              # هذا الملف
```

## 🔐 نظام الحماية

### منع التصويت المتكرر
1. **بصمة الجهاز (Device Fingerprinting)**
   - جمع معلومات عن الجهاز (المتصفح، نظام التشغيل، الدقة، إلخ)
   - إنشاء معرف فريد للجهاز
   - تخزينه في قاعدة البيانات

2. **تسجيل IP Address**
   - تسجيل عنوان IP الخاص بالجهاز
   - للمراجعة والتدقيق

3. **بيانات المتصفح**
   - تسجيل User Agent
   - معلومات المتصفح والجهاز

## 📋 المرشحون

1. **د أحمد حفظي**
2. **مهندس أحمد سامي**
3. **أ. أشرف الشبراوي**
4. **لواء سامح عبد الفتاح**
5. **أ.د مكرم رضوان**

## 🎯 كيفية الاستخدام

### للمصوتين
1. افتح الموقع
2. اختر المرشح الذي تريد التصويت له
3. اضغط "تأكيد التصويت"
4. سيظهر لك رسالة نجاح
5. لن تتمكن من التصويت مرة أخرى من نفس الجهاز

### لمشاهدة النتائج
- النتائج تظهر مباشرة أسفل الصفحة
- يمكن مشاهدتها في أي وقت

## ⚙️ التخصيص

### تغيير الإعدادات
- عدّل ملف `includes/config.php` لتغيير:
  - عنوان الموقع
  - أسماء المرشحين
  - بيانات قاعدة البيانات

### تغيير التصميم
- عدّل ملف `css/style.css` لتغيير الألوان والتصميم
- عدّل ملف `index.php` لتغيير الهيكل

## 🐛 استكشاف الأخطاء

### خطأ: "خطأ في الاتصال بقاعدة البيانات"
- تحقق من بيانات الاتصال في `setup.php`
- تأكد من أن قاعدة البيانات تعمل بشكل صحيح
- تحقق من أن اسم المستخدم وكلمة المرور صحيحة

### الموقع لا يعمل
- تأكد من أن PHP مفعّل على استضافتك
- تحقق من أن جميع الملفات موجودة
- افتح `setup.php` مرة أخرى لإعادة الإعداد

### الأصوات لا تُحفظ
- تحقق من أن قاعدة البيانات تعمل
- تأكد من أن جدول `votes` موجود
- حاول حذف ملف `setup.php` وإعادة رفعه

## 📞 الدعم

للمزيد من المعلومات:
- تحقق من ملفات الكود والتعليقات
- اقرأ رسائل الأخطاء بعناية
- تأكد من أن جميع الملفات موجودة

## 📝 ملاحظات مهمة

1. **الأمان:** لا تنسَ حذف ملف `setup.php` بعد الإعداد
2. **النسخ الاحتياطية:** قم بعمل نسخة احتياطية من قاعدة البيانات بانتظام
3. **التحديثات:** تحقق من التحديثات بانتظام

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**تم الإنشاء بواسطة:** نظام Manus  
**التاريخ:** نوفمبر 2025  
**الإصدار:** 1.0.0
